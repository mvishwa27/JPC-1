pipeline{
agent any

stages{
  stage('Build'){
      steps {
          echo "Hello.."
          sh "git checkout main"
          sh "git status"
          echo "present working directory:"
          sh "pwd"
          echo "my workspace is:"
          sh "echo ${WORKSPACE}"
      }
  }
}
}
