pipeline{

  agent any
  stages{
    
    stage('main_branch'){
    
        when {
          changeRequest target: 'master'
        }
       steps{
          echo "executing this stage if the build is on a change request..."
      }
    }
 
  }
}
