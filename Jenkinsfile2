pipeline{

  agent any
  stages{
    
    stage('main_branch'){
    
        when {
          changeRequest target: 'main'
        }
       steps{
          echo "executing this stage if the build is on a change request..."
      }
    }
 
  }
}
